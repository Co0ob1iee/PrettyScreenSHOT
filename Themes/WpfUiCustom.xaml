<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                    xmlns:helpers="clr-namespace:PrettyScreenSHOT.Helpers;assembly=PrettyScreenSHOT">

    <!--
    Custom WPF UI Styles for PrettyScreenSHOT

    This file contains custom styles that override or extend WPF UI defaults.
    It's used for maintaining brand identity and special control variations.
    -->

    <!-- Circular Icon Button (replacement for NeumorphicCircularButton) -->
    <Style x:Key="CircularIconButton" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- Small Circular Icon Button -->
    <Style x:Key="SmallCircularIconButton" TargetType="ui:Button" BasedOn="{StaticResource CircularIconButton}">
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- Large Circular Icon Button -->
    <Style x:Key="LargeCircularIconButton" TargetType="ui:Button" BasedOn="{StaticResource CircularIconButton}">
        <Setter Property="Width" Value="50"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="CornerRadius" Value="25"/>
        <Setter Property="FontSize" Value="18"/>
    </Style>

    <!-- Card Header Text Style -->
    <Style x:Key="CardHeaderText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>

    <!-- Card Subheader Text Style -->
    <Style x:Key="CardSubheaderText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Regular"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
    </Style>

    <!-- Subtle Card Style (less prominent than default) -->
    <Style x:Key="SubtleCard" TargetType="ui:Card" BasedOn="{StaticResource {x:Type ui:Card}}">
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>

    <!-- Compact Card Style -->
    <Style x:Key="CompactCard" TargetType="ui:Card" BasedOn="{StaticResource {x:Type ui:Card}}">
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <!-- StackPanel with consistent spacing using Attached Property -->
    <Style x:Key="StandardSpacing" TargetType="StackPanel">
        <Setter Property="helpers:StackPanelHelper.Spacing" Value="12"/>
    </Style>

    <Style x:Key="CompactSpacing" TargetType="StackPanel">
        <Setter Property="helpers:StackPanelHelper.Spacing" Value="8"/>
    </Style>

    <Style x:Key="WideSpacing" TargetType="StackPanel">
        <Setter Property="helpers:StackPanelHelper.Spacing" Value="16"/>
    </Style>

    <!-- Section separator -->
    <Style x:Key="SectionSeparator" TargetType="Border">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="0,16"/>
        <Setter Property="Background" Value="{DynamicResource DividerStrokeColorDefaultBrush}"/>
    </Style>

    <!--
    Custom accent colors (optional - uncomment and customize if needed)
    <SolidColorBrush x:Key="CustomAccentBrush" Color="#007ACC"/>
    <SolidColorBrush x:Key="CustomSuccessBrush" Color="#4CAF50"/>
    <SolidColorBrush x:Key="CustomWarningBrush" Color="#FF9800"/>
    <SolidColorBrush x:Key="CustomErrorBrush" Color="#F44336"/>
    -->

</ResourceDictionary>
