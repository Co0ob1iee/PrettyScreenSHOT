<ui:FluentWindow x:Class="PrettyScreenSHOT.Views.Dialogs.SaveScreenshotDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:helpers="clr-namespace:PrettyScreenSHOT.Helpers"
        Title="Save Screenshot"
        Height="450"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title bar -->
            <RowDefinition Height="*"/>    <!-- Main content -->
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0"
                     Title="Save Screenshot"
                     Icon="pack://application:,,,/app.ico"
                     ShowMaximize="False"/>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Input Fields Card -->
            <ui:Card Grid.Row="0">
                <StackPanel helpers:StackPanelHelper.Spacing="16">
                    <!-- Category -->
                    <StackPanel helpers:StackPanelHelper.Spacing="8">
                        <TextBlock x:Name="CategoryLabel"
                                   Text="Category:"
                                   FontWeight="SemiBold"/>
                        <ComboBox x:Name="CategoryComboBox"
                                  IsEditable="True"/>
                    </StackPanel>

                    <!-- Tags -->
                    <StackPanel helpers:StackPanelHelper.Spacing="8">
                        <TextBlock x:Name="TagsLabel"
                                   Text="Tags (comma separated):"
                                   FontWeight="SemiBold"/>
                        <ui:TextBox x:Name="TagsTextBox"
                                    PlaceholderText="e.g., work, important, bug"
                                    TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- Notes -->
                    <StackPanel helpers:StackPanelHelper.Spacing="8">
                        <TextBlock x:Name="NotesLabel"
                                   Text="Notes:"
                                   FontWeight="SemiBold"/>
                        <ui:TextBox x:Name="NotesTextBox"
                                    Height="80"
                                    TextWrapping="Wrap"
                                    AcceptsReturn="True"/>
                    </StackPanel>
                </StackPanel>
            </ui:Card>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        helpers:StackPanelHelper.Spacing="8"
                        Margin="0,16,0,0">
                <ui:Button x:Name="CancelButton"
                           Content="Cancel"
                           Click="OnCancelClick"
                           Icon="Dismiss24"
                           MinWidth="100"/>
                <ui:Button x:Name="SaveButton"
                           Content="Save"
                           Click="OnSaveClick"
                           Appearance="Primary"
                           Icon="Save24"
                           MinWidth="100"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
