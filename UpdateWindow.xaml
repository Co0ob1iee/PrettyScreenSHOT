<Window x:Class="PrettyScreenSHOT.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Available" Height="550" Width="620" WindowStartupLocation="CenterScreen"
        Background="Transparent" Foreground="{StaticResource NeumorphicTextBrush}"
        ShowInTaskbar="True" WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PrettyScreenSHOT;component/NeumorphicStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!-- Neumorphic window chrome -->
    <Border Style="{StaticResource NeumorphicPanel}" CornerRadius="18" Margin="15" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/> <!-- Title bar -->
                <RowDefinition Height="*"/>  <!-- Main content -->
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="Transparent" Padding="15,10" CornerRadius="18,18,0,0"
                    MouseLeftButtonDown="OnTitleBarMouseDown">
                <DockPanel>
                    <TextBlock Text="🔄 Update Available" FontSize="20"
                               Style="{StaticResource NeumorphicHeading}"
                               VerticalAlignment="Center" DockPanel.Dock="Left"/>

                    <Button Click="OnCloseClick" Style="{StaticResource NeumorphicCircularButton}"
                            Width="32" Height="32" DockPanel.Dock="Right"
                            HorizontalAlignment="Right" ToolTip="Zamknij">
                        <TextBlock Text="✕" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Button>
                </DockPanel>
            </Border>

            <!-- Main Content -->
            <DockPanel Grid.Row="1" Margin="20,10,20,20">
                <!-- Content -->
                <ScrollViewer DockPanel.Dock="Top" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15">
                    <StackPanel>
                        <!-- Subtitle -->
                        <TextBlock x:Name="SubtitleText" Text="A new version is available!"
                                   Style="{StaticResource NeumorphicTextBlockSecondary}"
                                   Margin="0,0,0,20"/>

                        <!-- Version Info -->
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Current Version:"
                                       Style="{StaticResource NeumorphicTextBlock}" FontWeight="Regular"/>
                            <TextBlock Grid.Column="1" x:Name="CurrentVersionText" Text="1.0.0"
                                       Style="{StaticResource NeumorphicTextBlock}"/>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="New Version:"
                                       Style="{StaticResource NeumorphicTextBlock}" FontWeight="Regular"/>
                            <TextBlock Grid.Column="1" x:Name="NewVersionText" Text="1.1.0"
                                       Style="{StaticResource NeumorphicTextBlock}"
                                       FontSize="16" FontWeight="Bold"/>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Release Date:"
                                       Style="{StaticResource NeumorphicTextBlock}" FontWeight="Regular"/>
                            <TextBlock Grid.Column="1" x:Name="ReleaseDateText" Text="2024-01-15"
                                       Style="{StaticResource NeumorphicTextBlock}"/>
                        </Grid>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="File Size:"
                                       Style="{StaticResource NeumorphicTextBlock}" FontWeight="Regular"/>
                            <TextBlock Grid.Column="1" x:Name="FileSizeText" Text="15.2 MB"
                                       Style="{StaticResource NeumorphicTextBlock}"/>
                        </Grid>

                        <!-- Release Notes -->
                        <TextBlock Text="Release Notes:"
                                   Style="{StaticResource NeumorphicTextBlock}"
                                   FontWeight="Regular" FontSize="14" Margin="0,10,0,5"/>
                        <Border Style="{StaticResource NeumorphicDepressedBorder}" Padding="12" MaxHeight="200">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <TextBlock x:Name="ReleaseNotesText" TextWrapping="Wrap"
                                          Text="Loading release notes..."
                                          Style="{StaticResource NeumorphicTextBlockSecondary}"/>
                            </ScrollViewer>
                        </Border>

                        <!-- Download Progress (hidden by default) -->
                        <StackPanel x:Name="ProgressPanel" Visibility="Collapsed" Margin="0,15,0,0">
                            <TextBlock Text="Downloading update..."
                                       Style="{StaticResource NeumorphicTextBlock}"
                                       FontWeight="Regular" Margin="0,0,0,5"/>
                            <Border Style="{StaticResource NeumorphicDepressedBorder}" Height="25" Padding="2">
                                <Border x:Name="DownloadProgressBar"
                                        Background="{StaticResource NeumorphicPressedBrush}"
                                        HorizontalAlignment="Left"
                                        Width="0"/>
                            </Border>
                            <TextBlock x:Name="ProgressText" Text="0%"
                                       Style="{StaticResource NeumorphicTextBlockSecondary}"
                                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <!-- Buttons -->
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button x:Name="SkipButton" Content="Skip This Version" Click="SkipButton_Click"
                            Style="{StaticResource NeumorphicRaisedButton}" Width="150"/>
                    <Button x:Name="LaterButton" Content="Later" Click="LaterButton_Click"
                            Style="{StaticResource NeumorphicRaisedButton}" Width="100" Margin="10,0,0,0"/>
                    <Button x:Name="DownloadButton" Content="Download Now" Click="DownloadButton_Click"
                            Style="{StaticResource NeumorphicDepressedButton}" Width="150" Margin="10,0,0,0"/>
                </StackPanel>
            </DockPanel>
        </Grid>
    </Border>
</Window>
