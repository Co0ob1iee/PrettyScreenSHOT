<Window x:Class="PrettyScreenSHOT.VideoCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Video Capture" Height="300" Width="500" WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="White"
        ShowInTaskbar="False" ResizeMode="NoResize">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <DockPanel Margin="20">
        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="#2D2D2D" BorderBrush="#404040" BorderThickness="0,0,0,1" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Video Capture" FontSize="20" FontWeight="Bold"/>
                <TextBlock x:Name="StatusText" Text="Ready to record" FontSize="12" Foreground="#CCCCCC" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Content -->
        <StackPanel>
            <!-- Recording Info -->
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Recording Area:" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" x:Name="AreaText" Text="Not selected"/>
            </Grid>
            
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Frame Rate:" FontWeight="Bold"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Slider x:Name="FrameRateSlider" Width="200" Minimum="1" Maximum="30" Value="10" 
                            VerticalAlignment="Center" Margin="0,0,10,0" ValueChanged="FrameRateSlider_ValueChanged"/>
                    <TextBlock x:Name="FrameRateLabel" Text="10 FPS" MinWidth="60"/>
                </StackPanel>
            </Grid>
            
            <Grid Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Format:" FontWeight="Bold"/>
                <ComboBox Grid.Column="1" x:Name="FormatComboBox" SelectedIndex="0">
                    <ComboBoxItem Content="GIF"/>
                    <ComboBoxItem Content="MP4"/>
                </ComboBox>
            </Grid>
            
            <!-- Recording Time -->
            <Grid Margin="0,0,0,15" Visibility="Collapsed" x:Name="RecordingInfoGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Recording Time:" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" x:Name="TimeText" Text="00:00" FontSize="18" FontWeight="Bold" Foreground="#60A5FA"/>
            </Grid>
            
            <!-- Frames Count -->
            <Grid Margin="0,0,0,15" Visibility="Collapsed" x:Name="FramesInfoGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Frames Captured:" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" x:Name="FramesText" Text="0" FontSize="14"/>
            </Grid>
        </StackPanel>
        
        <!-- Buttons -->
        <Border DockPanel.Dock="Bottom" Background="#2D2D2D" BorderBrush="#404040" BorderThickness="0,1,0,0" Padding="15" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="StartButton" Content="Start Recording" Click="StartButton_Click" 
                        Background="#4CAF50" Width="150"/>
                <Button x:Name="StopButton" Content="Stop Recording" Click="StopButton_Click" 
                        Background="#F44336" Width="150" IsEnabled="False"/>
                <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" 
                        Background="#9E9E9E" Width="100" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>
    </DockPanel>
</Window>

